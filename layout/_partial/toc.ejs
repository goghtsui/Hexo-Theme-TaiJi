<% if (!index && ((post.toc != false && !is_page() && theme.toc.on) || post.toc)) { %>
    <div id="toc" class="toc-article">
        <strong class="toc-title"><%= __('目录') %></strong>
        <% if ((theme.toc.list_number && post.toc_list_number != false) || post.toc_list_number) { %>
            <%- toc(post.content) %>
        <% } else { %>
            <%- toc(post.content, {list_number: false}) %>
        <% } %>
    </div>
    <style>
        .left-col .switch-btn,
        .left-col .switch-area {
            display: none;
        }
        .toc-level-<%= theme.toc.max_depth %> i,
        .toc-level-<%= theme.toc.max_depth %> ol {
            display: none !important;
        }
    </style>

    <input type="button" id="tocButton" value="<%= __('隐藏') %>"  title="<%= __('切换列表') %>">

    <script>
        yiliaConfig.toc = ["<%= __('隐藏') %>", "<%= __('显示') %>", !!"<%= theme.toc.nowrap %>"];
    </script>
<% } %>